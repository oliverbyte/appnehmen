name: Deploy to GitHub Pages

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Generate commit ID constant
        run: |
          COMMIT_ID=$(git rev-parse --short HEAD)
          mkdir -p lib/config
          echo "const String appCommitId = '$COMMIT_ID';" > lib/config/commit_id.dart

      - name: Build web
        run: flutter build web --release --base-href "/appnehmen/"

      - name: Update Service Worker cache version
        run: |
          COMMIT_ID=$(git rev-parse --short HEAD)
          sed -i "s/const CACHE_NAME = 'appnehmen-cache-v[^']*'/const CACHE_NAME = 'appnehmen-cache-v$COMMIT_ID'/" build/web/service_worker.js

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
